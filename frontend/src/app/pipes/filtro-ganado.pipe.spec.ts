import { FiltroGanadoPipe } from './filtro-ganado.pipe';

describe('FiltroGanadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGanadoPipe();
    expect(pipe).toBeTruthy();
  });
});
